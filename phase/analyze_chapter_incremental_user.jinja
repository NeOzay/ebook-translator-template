{#- ================================================================
ANALYZE_CHAPTER_INCREMENTAL_USER.JINJA - Phase 0 : Analyse incrémentale (User)

Purpose: User prompt with existing partial analysis and new text block.

Input variables:
  - chapter_name
  - current_block
  - total_blocks
  - partial_analysis_json
  - block_text
  - is_last_block

Pair: analyze_chapter_incremental_system.jinja
================================================================ -#}

Chapitre : "{{ chapter_name }}" — Bloc {{ current_block }}/{{ total_blocks }}{% if is_last_block %} (dernier bloc){% endif %}

Analyse existante :
```json
{{ partial_analysis_json }}
```

Nouveau bloc de texte :

{{ block_text }}
